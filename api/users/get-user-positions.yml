info:
  name: get-user-positions
  type: http
  seq: 5

http:
  method: GET
  url: "{{api-gateway-base-url}}/users/positions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
