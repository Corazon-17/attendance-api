info:
  name: refresh-token
  type: http
  seq: 2

http:
  method: POST
  url: "{{api-gateway-base-url}}/auth/refresh"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
